name: test

channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - black
  - codecov
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - bibtexparser<2

