name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - codecov
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - bibtexparser<2
    
      # Testing
    - yapf

